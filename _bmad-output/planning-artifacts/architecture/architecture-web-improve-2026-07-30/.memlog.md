---
scope: Port SkillForge sang Vue 3 + Vite theo strangler; hai app sống song song
purpose: build-substrate
altitude: feature
updated: 2026-07-30T13:21
---

- (constraint) [từ PRD] Vue 3 + Vite tại projects/web-app; Legacy app tại projects/web-en LUÔN chạy được; API server giữ zero-dependency; không TypeScript; không thư viện component ngoài; không SSR
- (version) Kiểm registry npm 2026-07-30 (không lấy từ trí nhớ): vue 3.5.40 · vite 8.1.5 (latest, previous 7.3.6) · vue-router 5.2.0 (latest stable) · @vitejs/plugin-vue 6.0.8 (peer nhận vite ^8) · node cục bộ v22.23.1 thoả engines vite ^20.19||>=22.12
- (decision) OQ#1 → Vue import logic trực tiếp từ web-en qua Vite alias @legacy. Không di chuyển file, không package mới, Legacy app không biết gì nên không thể vỡ. Đổi lấy: web-app phụ thuộc thư mục sẽ bị xoá ở cuối cuộc port (nợ có tên, dọn ở đợt cuối)
- (decision) OQ#2 → 12 trang nội dung nặng KHÔNG port; Legacy phục vụ tiếp. Căn cứ đo được: 0 chỗ tương tác (grep onclick/addEventListener/script = 0 ở cloud.html, java/backend, java/spring-boot, 5 trang frontend), thuần <pre> code sample. Cắt ~3.500 dòng HTML khỏi khối lượng port mà người dùng không mất gì
- (decision) OQ#3 → giữ nguyên ba nơi lưu; Vue ghi đúng nơi tính năng tương ứng đang ghi (quiz→quizHistory, exam→skillforge_exam_history, phiên→IndexedDB SkillForgeProgress). Không migrate đợt này vì dữ liệu nằm trong browser người dùng, migrate sai là mất
- (direction) Fast path theo yêu cầu user; nhưng ba quyết định xương sống trên được đưa ra kèm phương án thay thế để user chọn, không tự chốt
- (event) Deliverable = chỉ spine, không cần bản trình bày cho người đọc
- (decision) AD-1..AD-15 distill từ memlog; paradigm = strangler fig façade trên client phân lớp một chiều, 4 lớp pages→components→logic→adapters
- (event) lint_spine.py PASS 0 finding
- (version) Kiểm chéo tương thích: vue-router@5.2.0 peer vue ^3.5.34||^4 (vue 3.5.40 thoả) · plugin-vue@6.0.8 peer vite ^5..^8 (vite 8.1.5 thoả) · peer pinia/@pinia-colada/vite/compiler-sfc của vue-router đều optional:true nên Deferred hoãn Pinia không xung đột · vue 3.6 còn ở rc, latest vẫn 3.5.40
- (decision) Lens adversarial tìm LỖ A: AD-4 (logic giữ nguyên) mâu thuẫn AD-6 (chỉ adapter chạm storage) vì quiz-logic.js:70,82 và skill-logic.js:44,52,58 gọi localStorage trực tiếp → siết AD-6: luật chỉ áp trong web-app/, nơi lưu nào logic Legacy đã ghi thì Vue đi qua chính module đó, không dựng adapter song song
- (decision) Lens adversarial tìm LỖ B: không AD nào chi phối cách Vue với tới DỮ LIỆU Legacy (10 file js/data xuất bằng window.* thay vì export) → thêm AD-16: chỉ import ESM từ @legacy/data, file chưa export thì THÊM dòng export cạnh phép gán window.*; cấm ?raw, eval, khai lại dữ liệu
- (change) AD-15 mở allowlist sửa web-en từ 3 lên 4 nhóm (thêm nhóm d: thêm export vào file dữ liệu theo AD-16)
- (question) [DEFERRED] Cơ chế nối hai app khi deploy — AD-2 ghim một origin nhưng cách đạt (reverse proxy vs build Vue vào thư mục Legacy) chưa chốt; xét lại khi có ý định deploy
- (question) [DEFERRED] Hợp nhất 3 nơi lưu + migrate IndexedDB v1→v2 — xét lại khi Legacy app sắp bị xoá và chỉ còn một chủ
- (question) [DEFERRED] Test tự động — thành chặn ngay nếu mục tiêu chuyển từ cá nhân sang portfolio
- (event) spine finalized
- (event) Reviewer Gate xong: lint PASS, version PASS, adversarial 2 lỗ đã bịt
