// DevOps learning content — song ngữ EN-VI
window.devopsTopics = [
  {
    "file": "README.md",
    "title": "📚 DevOps — Tổng quan",
    "content": "## 🎯 DevOps là gì? (What is DevOps?)\n\nDevOps là văn hóa + bộ công cụ giúp phát triển (Development) và vận hành (Operations) phối hợp chặt chẽ — tự động hóa toàn bộ vòng đời phần mềm từ code đến production.\n\n*DevOps is a culture and toolset that bridges Development and Operations — automating the whole software lifecycle from code to production.*\n\n## 📑 Lộ trình (Roadmap)\n\n| Chủ đề | Nội dung chính |\n|---|---|\n| 1. **Git & GitHub** | Version control, branching, PR, GitHub Actions cơ bản |\n| 2. **Docker** | Image, Container, Dockerfile, Compose, Volume |\n| 3. **Kubernetes** | Pod, Deployment, Service, ConfigMap, HPA |\n| 4. **CI/CD** | Pipeline, Jenkins, GitHub Actions, deploy strategies |\n| 5. **Terraform** | HCL, state, plan/apply, modules, remote state |\n| 6. **Monitoring** | Prometheus, Grafana, CloudWatch, golden signals |\n| 7. **AWS Core** | EC2, Lambda, S3, RDS, VPC, IAM |\n| 8. **Practice** | Câu hỏi phỏng vấn + tình huống thực tế |\n\n## 🚀 Mục tiêu (Goals)\n\n- Hiểu vòng đời DevOps: **code → build → test → deploy → monitor**\n- Thành thạo Docker + K8s để containerize & orchestrate\n- Xây dựng CI/CD pipeline tự động\n- Quản lý hạ tầng bằng code (Terraform)\n- Giám sát hệ thống bằng metrics/logs/traces",
    "checklist": []
  },
  {
    "file": "Devops1_Git_GitHub.md",
    "title": "📄 CHỦ ĐỀ 1 — GIT & GITHUB",
    "content": "---\n\n## 1. Git là gì? (What is Git?)\n\nGit là distributed version control system — lưu lịch sử thay đổi của code, cho phép nhiều người làm việc song song. *Git is a distributed version control system that tracks code changes and enables parallel collaboration.*\n\n| Thuật ngữ | Ý nghĩa (Meaning) |\n|---|---|\n| `repository` | Nơi lưu toàn bộ code + lịch sử (kho chứa) |\n| `commit` | Một snapshot của code tại thời điểm (ảnh chụp) |\n| `branch` | Nhánh làm việc độc lập (nhánh) |\n| `merge` | Gộp nhánh này vào nhánh khác (gộp) |\n| `remote` | Bản sao repo trên server (GitHub, GitLab) |\n| `HEAD` | Con trỏ tới commit hiện tại (điểm đứng hiện tại) |\n\n## 2. Quy trình Git cơ bản (Basic Git Workflow)\n\n```bash\n# Lấy repo về máy\ngit clone git@github.com:user/repo.git\n\n# Kiểm tra trạng thái\ngit status\n\n# Thêm file vào staging area\ngit add .\n\n# Commit với message rõ ràng\ngit commit -m \"feat: add login API\"\n\n# Đẩy lên remote\ngit push origin main\n\n# Lấy thay đổi mới từ remote\ngit pull\n```\n\n## 3. Branching — Làm việc theo nhánh\n\n*Branches let you develop features in isolation without affecting the main codebase.*\n\n```bash\ngit checkout -b feature/login    # Tạo + chuyển sang nhánh mới\ngit branch                       # Danh sách nhánh\ngit checkout main                # Chuyển về nhánh chính\ngit merge feature/login          # Gộp nhánh feature vào main\n\n# Xóa nhánh sau khi gộp\ngit branch -d feature/login\ngit push origin --delete feature/login\n```\n\n## 4. Git Flow chuẩn (Standard Git Flow)\n\n```\nmain (production, ổn định)\n └── develop (tích hợp)\n      ├── feature/xxx   ← mỗi tính năng 1 nhánh\n      ├── release/1.0   ← chuẩn bị release\n      └── hotfix/urgent ← sửa lỗi production khẩn cấp\n```\n\n- **Trunk-based**: nhánh ngắn, merge về main liên tục (ưa chuộng với CI/CD)\n- **Git Flow**: nhiều nhánh, phù hợp release theo chu kỳ\n\n## 5. Pull Request (PR)\n\n*PR is the way to review code before merging — a quality gate for collaboration.*\n\n1. Tạo nhánh `feature/xxx`\n2. Commit + push nhánh lên GitHub\n3. Mở **Pull Request** → đồng nghiệp review + comment\n4. CI chạy test tự động (GitHub Actions)\n5. Merge khi đạt yêu cầu\n\n## 6. Rebase vs Merge\n\n- `git merge`: giữ lịch sử gốc, tạo merge commit — lịch sử nhiều nhánh\n- `git rebase`: dồn commit lên đỉnh nhánh khác — lịch sử thẳng hàng, sạch hơn\n\n```bash\n# Rebase nhánh feature lên main mới nhất\ngit checkout feature\ngit rebase main\n```\n\n## 📝 Checklist\n\n- [ ] Cloning repository với git clone\n- [ ] Thành thạo add/commit/push/pull\n- [ ] Tạo và gộp branch, xử lý merge conflict\n- [ ] Mở Pull Request và review code\n- [ ] Phân biệt merge vs rebase",
    "checklist": [
      "Cloning repository với git clone",
      "Thành thạo add/commit/push/pull",
      "Tạo và gộp branch, xử lý merge conflict",
      "Mở Pull Request và review code",
      "Phân biệt merge vs rebase"
    ]
  },
  {
    "file": "Devops2_Docker.md",
    "title": "📄 CHỦ ĐỀ 2 — DOCKER",
    "content": "---\n\n## 1. Image vs Container\n\n*An image is a read-only template; a container is a running instance of it.*\n\n| **Image** | **Container** |\n|---|---|\n| Template read-only (bản mẫu chỉ-đọc) | Instance đang chạy (thực thể chạy) |\n| Giống class trong OOP | Giống object từ class |\n| Chia sẻ qua registry | Có lifecycle: start/stop/delete |\n\n## 2. Lệnh cơ bản (Basic Commands)\n\n```bash\ndocker pull nginx:alpine       # Tải image\ndocker images                  # Xem images\ndocker run -d -p 8080:80 --name web nginx\ndocker ps                      # Containers đang chạy\ndocker logs web                # Xem log\ndocker exec -it web sh         # Vào container\ndocker stop web && docker rm web\ndocker rmi nginx:alpine        # Xóa image\n```\n\n## 3. Dockerfile\n\n```dockerfile\nFROM eclipse-temurin:17-jdk-alpine   # Base image nhẹ\nWORKDIR /app\nCOPY target/app.jar app.jar\nEXPOSE 8080\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]\n```\n\n- Mỗi lệnh tạo 1 **layer** → Docker cache từng layer, build nhanh\n- Đặt lệnh ít thay đổi (cài deps) lên trước\n\n## 4. Docker Compose\n\n*Multi-container apps defined in one YAML file.*\n\n```yaml\nversion: '3.8'\nservices:\n  app:\n    build: .\n    ports: [\"8080:8080\"]\n    depends_on:\n      - db\n  db:\n    image: postgres:16-alpine\n    volumes:\n      - pgdata:/var/lib/postgresql/data\nvolumes:\n  pgdata:\n```\n\n## 5. Volume — Dữ liệu bền vững\n\n*Container filesystem is ephemeral — use volumes for persistent data.*\n\n```bash\n# Named volume (khuyến nghị cho DB)\ndocker run -d -v pgdata:/var/lib/postgresql/data postgres\n\n# Bind mount (dev, hot-reload)\ndocker run -d -v $(pwd)/src:/app/src node:20\n```\n\n## 6. Multi-stage Build\n\n*Separate build and runtime stages — smaller, safer final image.*\n\n```dockerfile\nFROM node:20-alpine AS builder\nWORKDIR /app\nCOPY . .\nRUN npm ci && npm run build\n\nFROM nginx:alpine\nCOPY --from=builder /app/dist /usr/share/nginx/html\n```\n\n## 📝 Checklist\n\n- [ ] Phân biệt Image vs Container\n- [ ] Viết Dockerfile và build image\n- [ ] Dùng Docker Compose cho app nhiều service\n- [ ] Dùng volume để giữ dữ liệu\n- [ ] Áp dụng multi-stage build\n- [ ] Biết các lệnh cơ bản docker run/ps/logs/exec",
    "checklist": [
      "Phân biệt Image vs Container",
      "Viết Dockerfile và build image",
      "Dùng Docker Compose cho app nhiều service",
      "Dùng volume để giữ dữ liệu",
      "Áp dụng multi-stage build",
      "Biết các lệnh cơ bản docker run/ps/logs/exec"
    ]
  },
  {
    "file": "Devops3_Kubernetes.md",
    "title": "📄 CHỦ ĐỀ 3 — KUBERNETES",
    "content": "---\n\n## 1. K8s là gì? (What is Kubernetes?)\n\nKubernetes (K8s) là container orchestration platform — tự động triển khai, scale, tự phục hồi ứng dụng containerized trên nhiều máy. *K8s is a container orchestration platform that automates deployment, scaling, and self-healing of containerized apps.*\n\n## 2. Kiến trúc (Architecture)\n\n```\nControl Plane (điều khiển)\n├── API Server   — cửa ngõ duy nhất, mọi kubectl đi qua\n├── etcd         — kho lưu trạng thái\n├── Scheduler    — chọn node chạy pod\n└── Controller   — giữ desired state\n\nWorker Nodes (chạy app)\n├── kubelet      — agent quản lý pods\n├── kube-proxy   — network + load balancing\n└── Pods         — đơn vị nhỏ nhất (1+ containers)\n```\n\n## 3. Pod\n\n*The smallest deployable unit — one or more containers sharing network and storage.*\n\n```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: nginx-pod\n  labels: { app: nginx }\nspec:\n  containers:\n    - name: nginx\n      image: nginx:alpine\n      ports: [{ containerPort: 80 }]\n```\n\n## 4. Deployment\n\n*Manages ReplicaSets — declarative updates, scaling, rollback.*\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: myapp\nspec:\n  replicas: 3\n  selector:\n    matchLabels: { app: myapp }\n  template:\n    metadata:\n      labels: { app: myapp }\n    spec:\n      containers:\n        - name: myapp\n          image: myapp:1.0\n          ports: [{ containerPort: 8080 }]\n          resources:\n            requests: { cpu: \"100m\", memory: \"128Mi\" }\n            limits:   { cpu: \"500m\", memory: \"512Mi\" }\n```\n\n```bash\nkubectl apply -f deployment.yaml\nkubectl get pods\nkubectl rollout status deployment/myapp\nkubectl set image deployment/myapp myapp=myapp:1.1\nkubectl rollout undo deployment/myapp   # Rollback\n```\n\n## 5. Service & Ingress\n\n*Service = stable IP + load balancing giữa các pods.*\n\n| Type | Dùng khi |\n|---|---|\n| **ClusterIP** (mặc định) | Nội bộ cluster — microservices gọi nhau |\n| **NodePort** | Mở port cố định trên node (30000-32767) |\n| **LoadBalancer** | Cloud LB ra internet |\n\n```yaml\napiVersion: v1\nkind: Service\nmetadata: { name: myapp }\nspec:\n  selector: { app: myapp }\n  ports:\n    - { port: 80, targetPort: 8080 }\n  type: ClusterIP\n```\n\n## 6. ConfigMap & Secret\n\n*ConfigMap: cấu hình không bí mật. Secret: dữ liệu nhạy cảm (base64).*\n\n```yaml\napiVersion: v1\nkind: ConfigMap\nmetadata: { name: app-config }\ndata:\n  APP_ENV: \"production\"\n```\n\n```yaml\napiVersion: v1\nkind: Secret\nmetadata: { name: db-secret }\ntype: Opaque\ndata:\n  DB_PASSWORD: c2VjcmV0MTIz   # base64\n```\n\n## 7. HPA — Autoscaling\n\n*Horizontal Pod Autoscaler scales replicas based on CPU/memory.*\n\n```yaml\napiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata: { name: myapp-hpa }\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: myapp\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target: { type: Utilization, averageUtilization: 70 }\n```\n\n## 📝 Checklist\n\n- [ ] Giải thích kiến trúc Control Plane vs Worker Node\n- [ ] Viết manifest Pod và Deployment\n- [ ] Dùng Service ClusterIP/NodePort/LoadBalancer\n- [ ] Tạo ConfigMap và Secret, inject vào pod\n- [ ] Cấu hình HPA auto-scaling\n- [ ] Thành thạo kubectl cơ bản",
    "checklist": [
      "Giải thích kiến trúc Control Plane vs Worker Node",
      "Viết manifest Pod và Deployment",
      "Dùng Service ClusterIP/NodePort/LoadBalancer",
      "Tạo ConfigMap và Secret, inject vào pod",
      "Cấu hình HPA auto-scaling",
      "Thành thạo kubectl cơ bản"
    ]
  },
  {
    "file": "Devops4_CICD.md",
    "title": "📄 CHỦ ĐỀ 4 — CI/CD",
    "content": "---\n\n## 1. CI/CD là gì? (What is CI/CD?)\n\n**CI — Continuous Integration:** build + test tự động mỗi lần commit → phát hiện lỗi sớm. *Auto build and test on every commit — catch bugs early.*\n\n**CD — Continuous Delivery/Deployment:** tự động đưa artifact đạt chuẩn lên production. *Automatically ship tested artifacts to production.*\n\n```\nCode commit → Build → Test → Quality gate → Build image → Deploy staging → Deploy prod\n[CI]                                                        [CD]\n```\n\n## 2. Pipeline Stages\n\n| Stage | Việc làm |\n|---|---|\n| Checkout | Lấy source từ git |\n| Install | Cài dependencies |\n| Lint | Kiểm tra code style |\n| Test | Unit + integration test |\n| Build | Compile, đóng gói artifact |\n| Image | Docker build + push registry |\n| Deploy | Triển khai staging → prod |\n\n## 3. GitHub Actions\n\n*.github/workflows/ci.yml*\n\n```yaml\nname: CI\non:\n  push:\n    branches: [ main ]\n  pull_request:\n    branches: [ main ]\n\njobs:\n  build:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-java@v4\n        with:\n          distribution: temurin\n          java-version: '17'\n      - name: Build\n        run: mvn -B package\n```\n\n## 4. Jenkins — Jenkinsfile\n\n```groovy\npipeline {\n    agent any\n    stages {\n        stage('Build') { steps { sh 'mvn -B package' } }\n        stage('Test')  { steps { sh 'mvn test' } }\n        stage('Deploy'){ steps { sh './deploy.sh' } }\n    }\n}\n```\n\n## 5. Deploy Strategies\n\n| Strategy | Cách hoạt động | Rollback |\n|---|---|---|\n| **Rolling** | Thay dần instance cũ bằng mới | Trung bình |\n| **Blue/Green** | 2 môi trường, chuyển toàn bộ traffic | Nhanh (đổi traffic) |\n| **Canary** | 5-10% traffic → version mới, tăng dần | Nhanh (tắt canary) |\n\n*Canary là an toàn nhất — phát hiện lỗi với rủi ro tối thiểu.*\n\n## 6. GitOps\n\n*Git là single source of truth; ArgoCD/Flux tự đồng bộ cluster khớp với git.*\n\n- Mọi thay đổi = Pull Request → merge\n- Rollback = revert commit\n- Cluster tự phục hồi (self-healing)\n\n## 📝 Checklist\n\n- [ ] Phân biệt CI, CD Delivery, CD Deployment\n- [ ] Viết GitHub Actions workflow\n- [ ] Viết Jenkinsfile cơ bản\n- [ ] So sánh rolling / blue-green / canary\n- [ ] Giải thích GitOps và ArgoCD",
    "checklist": [
      "Phân biệt CI, CD Delivery, CD Deployment",
      "Viết GitHub Actions workflow",
      "Viết Jenkinsfile cơ bản",
      "So sánh rolling / blue-green / canary",
      "Giải thích GitOps và ArgoCD"
    ]
  },
  {
    "file": "Devops5_Terraform.md",
    "title": "📄 CHỦ ĐỀ 5 — TERRAFORM",
    "content": "---\n\n## 1. IaC & Terraform\n\n*Infrastructure as Code: quản lý hạ tầng bằng code thay vì bấm tay console.*\n\n- **Declarative**: khai báo kết quả mong muốn, Terraform tính ra cách làm\n- **Multi-cloud**: AWS, GCP, Azure, K8s qua provider\n- **State-aware**: biết tài nguyên nào đang tồn tại, tránh tạo trùng\n\n## 2. Cấu trúc HCL\n\n```hcl\nterraform {\n  required_providers {\n    aws = {\n      source  = \"hashicorp/aws\"\n      version = \"~> 5.0\"\n    }\n  }\n}\n\nprovider \"aws\" {\n  region = \"ap-southeast-1\"\n}\n\nresource \"aws_instance\" \"web\" {\n  ami           = \"ami-0c55b159cbfafe1f0\"\n  instance_type = \"t3.micro\"\n  tags = { Name = \"web-server\" }\n}\n```\n\n| Khối | Ý nghĩa |\n|---|---|\n| `terraform {}` | Version, providers, backend |\n| `provider \"\"` | Cấu hình cloud provider |\n| `resource \"\" \"\"` | Tài nguyên cụ thể (tạo mới) |\n| `data \"\" \"\"` | Đọc tài nguyên đã tồn tại |\n| `variable` / `output` | Input / giá trị trả ra |\n\n## 3. Workflow: init → plan → apply\n\n```bash\nterraform init                 # Tải providers + backend\nterraform fmt                  # Format code\nterraform validate             # Kiểm tra syntax\nterraform plan -out plan.tfplan # Xem diff trước khi thay đổi\nterraform apply plan.tfplan    # Thực thi thay đổi\nterraform destroy              # Xóa toàn bộ hạ tầng\n```\n\n*Luôn review plan trước apply — dòng -/+ nghĩa là destroy + recreate (có thể downtime).*\n\n## 4. State & Remote State\n\n*State file ghi ánh xạ code → tài nguyên thực tế. Nên lưu remote để chia sẻ team + locking.*\n\n```hcl\nterraform {\n  backend \"s3\" {\n    bucket         = \"my-terraform-state\"\n    key            = \"prod/network/terraform.tfstate\"\n    region         = \"ap-southeast-1\"\n    dynamodb_table = \"terraform-locks\"\n    encrypt        = true\n  }\n}\n```\n\n## 5. Modules\n\n*Self-contained reusable packages with inputs/outputs.*\n\n```hcl\nmodule \"vpc\" {\n  source  = \"terraform-aws-modules/vpc/aws\"\n  version = \"5.0.0\"\n  name    = \"my-vpc\"\n  cidr    = \"10.0.0.0/16\"\n}\n```\n\n## 6. Variables & Outputs\n\n```hcl\nvariable \"environment\" {\n  type    = string\n  default = \"dev\"\n}\n\noutput \"instance_ip\" {\n  value = aws_instance.web.public_ip\n}\n```\n\n## 📝 Checklist\n\n- [ ] Giải thích IaC declarative vs imperative\n- [ ] Viết resource/provider cơ bản bằng HCL\n- [ ] Hiểu workflow init → plan → apply\n- [ ] Giải thích state file và remote state\n- [ ] Dùng module tái sử dụng\n- [ ] Tránh commit secret vào state/code",
    "checklist": [
      "Giải thích IaC declarative vs imperative",
      "Viết resource/provider cơ bản bằng HCL",
      "Hiểu workflow init → plan → apply",
      "Giải thích state file và remote state",
      "Dùng module tái sử dụng",
      "Tránh commit secret vào state/code"
    ]
  },
  {
    "file": "Devops6_Monitoring.md",
    "title": "📄 CHỦ ĐỀ 6 — MONITORING & OBSERVABILITY",
    "content": "---\n\n## 1. Monitoring vs Observability\n\n- **Monitoring**: detect vấn đề đã biết qua threshold (\"có gì sai không?\")\n- **Observability**: hiểu trạng thái hệ thống qua đầu ra để debug vấn đề mới (\"tại sao sai?\")\n\n## 2. Ba trụ cột (Three Pillars)\n\n| Trụ cột | Mô tả | Công cụ |\n|---|---|---|\n| **Metrics** 📈 | Số liệu theo thời gian (CPU, latency, error rate) | Prometheus, CloudWatch |\n| **Logs** 📜 | Sự kiện chi tiết dạng text | ELK, Loki |\n| **Traces** 🔗 | Hành trình request qua services | Jaeger, X-Ray |\n\n## 3. Golden Signals (Google SRE)\n\n1. **Latency** — thời gian phản hồi (đo p95/p99, không chỉ average)\n2. **Traffic** — lượng request (RPS)\n3. **Errors** — rate lỗi 5xx / business errors\n4. **Saturation** — mức độ đầy (CPU, memory, queue depth)\n\n## 4. Prometheus\n\n*Pull-based time-series monitoring + PromQL query.*\n\n```yaml\nscrape_configs:\n  - job_name: 'myapp'\n    metrics_path: /actuator/prometheus\n    static_configs:\n      - targets: ['myapp:8080']\n```\n\n```promql\n# Request rate 5 phút\nrate(http_server_requests_seconds_count[5m])\n\n# Error rate %\nsum(rate(http_server_requests_seconds_count{status=~\"5..\"}[5m]))\n  / sum(rate(http_server_requests_seconds_count[5m])) * 100\n\n# p95 latency\nhistogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket[5m])) by (le))\n```\n\n## 5. Grafana\n\n*Dashboard platform — nguồn dữ liệu từ Prometheus, Loki, CloudWatch...*\n\n- Dashboards + panels (graph, gauge, heatmap)\n- Alerting → Slack/Email/PagerDuty\n- Variables cho dashboard tương tác\n\n## 6. AWS CloudWatch\n\n- **Metrics**: CPU, NetworkIn/Out EC2; Duration/Errors Lambda\n- **Logs**: tập trung log, query bằng Logs Insights\n- **Alarms**: threshold → SNS → email/Slack\n- **X-Ray**: distributed tracing\n- **CloudTrail**: audit mọi API call\n\n## 📝 Checklist\n\n- [ ] Phân biệt Monitoring vs Observability\n- [ ] Nêu 3 trụ cột metrics/logs/traces\n- [ ] Nêu 4 golden signals\n- [ ] Viết PromQL query cơ bản (rate, error rate, p95)\n- [ ] Giải thích CloudWatch metrics/logs/alarms\n- [ ] Hiểu vì sao p95/p99 quan trọng hơn average",
    "checklist": [
      "Phân biệt Monitoring vs Observability",
      "Nêu 3 trụ cột metrics/logs/traces",
      "Nêu 4 golden signals",
      "Viết PromQL query cơ bản (rate, error rate, p95)",
      "Giải thích CloudWatch metrics/logs/alarms",
      "Hiểu vì sao p95/p99 quan trọng hơn average"
    ]
  },
  {
    "file": "Devops7_AWS_Core.md",
    "title": "📄 CHỦ ĐỀ 7 — AWS CORE",
    "content": "---\n\n## 1. Compute\n\n| Dịch vụ | Dùng khi |\n|---|---|\n| **EC2** | VM truyền thống, kiểm soát đầy đủ, chạy lâu dài |\n| **ECS / EKS** | Container orchestration (Fargate / K8s) |\n| **Lambda** | Serverless theo event, tự scale, trả theo ms |\n| **ELB / ALB** | Load balancing traffic vào app |\n\n## 2. Storage & Database\n\n- **S3**: object storage, độ bền 11 nines — artifact, backup, static website\n- **RDS**: SQL quan hệ (PostgreSQL, MySQL...) — transaction, join\n- **DynamoDB**: NoSQL key-value — latency thấp, scale ngang lớn\n- **ElastiCache**: Redis/Memcached — cache, session\n\n## 3. Networking & Security\n\n- **VPC**: mạng ảo riêng — public subnet (IGW) + private subnet (NAT Gateway)\n- **Security Group**: stateful firewall cấp instance\n- **IAM**: Users, Roles, Policies — quyền truy cập\n- **Route 53**: DNS\n- **CloudFront**: CDN\n\n```json\n// IAM policy — EC2 đọc S3\n{\n  \"Version\": \"2012-10-17\",\n  \"Statement\": [{\n    \"Effect\": \"Allow\",\n    \"Action\": [\"s3:GetObject\"],\n    \"Resource\": [\"arn:aws:s3:::my-bucket/*\"]\n  }]\n}\n```\n\n## 4. AWS DevOps Stack\n\n```\nCodeCommit → CodeBuild → CodePipeline → CodeDeploy\n```\n\n- **CodePipeline**: orchestrate release\n- **CodeBuild**: managed build\n- **CodeDeploy**: deploy EC2/ECS/Lambda (rolling, blue/green)\n- **CloudFormation**: IaC native AWS\n\n## 5. Kiến trúc tham khảo (Reference Architecture)\n\n```\nRoute 53 → CloudFront → ALB → EC2/ECS (multi-AZ) → RDS\n                        └→ S3 (static/artifacts)\n                        └→ Lambda (serverless features)\n```\n\n## 📝 Checklist\n\n- [ ] Phân biệt EC2, ECS/EKS, Lambda\n- [ ] Chọn S3 vs RDS vs DynamoDB đúng bài toán\n- [ ] Hiểu VPC public/private subnet\n- [ ] Viết IAM policy cơ bản\n- [ ] Mô tả bộ CI/CD AWS (CodePipeline...)\n- [ ] Vẽ kiến trúc AWS cơ bản",
    "checklist": [
      "Phân biệt EC2, ECS/EKS, Lambda",
      "Chọn S3 vs RDS vs DynamoDB đúng bài toán",
      "Hiểu VPC public/private subnet",
      "Viết IAM policy cơ bản",
      "Mô tả bộ CI/CD AWS (CodePipeline...)",
      "Vẽ kiến trúc AWS cơ bản"
    ]
  },
  {
    "file": "Devops8_Practice.md",
    "title": "📄 CHỦ ĐỀ 8 — LUYỆN TẬP & PHỎNG VẤN",
    "content": "---\n\n## 1. Câu hỏi phỏng vấn (Interview Questions)\n\n### Docker\n- Image vs Container khác gì? → Template read-only vs instance đang chạy\n- CMD vs ENTRYPOINT? → CMD bị ghi đè khi run, ENTRYPOINT không\n- Container khác VM? → Container share kernel host, VM có guest OS riêng\n\n### Kubernetes\n- Pod vs Container? → Pod là đơn vị nhỏ nhất, có thể chứa 1+ containers\n- Deployment vs StatefulSet? → Stateless vs stateful (identity ổn định, storage riêng)\n- Service types? → ClusterIP / NodePort / LoadBalancer\n- Làm sao scale tự động? → HPA (replicas), Cluster Autoscaler (nodes), VPA (resources)\n\n### CI/CD\n- CI vs CD? → CI build+test mỗi commit; CD tự động deploy\n- Blue/Green vs Canary? → Chuyển toàn bộ vs từng phần traffic\n- GitOps là gì? → Git là source of truth, ArgoCD tự sync\n\n### Terraform\n- Terraform vs Ansible? → Provisioning (Terraform) vs configuration (Ansible)\n- State file là gì? → Ánh xạ code→tài nguyên, nên lưu remote\n- plan vs apply? → Tính diff (an toàn) vs thực thi thay đổi\n\n### Monitoring\n- 3 trụ cột observability? → Metrics, Logs, Traces\n- 4 golden signals? → Latency, Traffic, Errors, Saturation\n- Tại sao p95 quan trọng? → Average che giấu request chậm hiếm gặp\n\n### AWS\n- EC2 vs Lambda? → VM kiểm soát đầy đủ vs serverless theo event\n- RDS vs DynamoDB? → SQL quan hệ vs NoSQL key-value\n- IAM Role vs User? → Quyền tạm thời cho service vs credentials cố định cho người\n\n## 2. Tình huống thực tế (Real Scenarios)\n\n### 💡 App chậm khi tăng tải, bạn xử lý thế nào?\n1. Xem **golden signals**: latency tăng ở đâu? error rate?\n2. Kiểm tra **saturation**: CPU/RAM/DB connections có đầy không?\n3. Xem **logs + traces**: service nào là bottleneck?\n4. Scale: HPA tăng replica, hoặc tối ưu query/cache\n\n### 💡 Deploy version mới gây lỗi production?\n1. Rollback ngay: `kubectl rollout undo` hoặc revert commit (GitOps)\n2. Chuyển traffic về blue (nếu dùng blue/green)\n3. Phân tích RCA từ logs/traces, thêm quality gate ngăn tái diễn\n\n### 💡 Team cần chuẩn hóa môi trường dev/prod?\n1. Containerize bằng Docker → môi trường nhất quán\n2. Quản lý cấu hình bằng Terraform → hạ tầng như code\n3. CI/CD pipeline tự build/test/deploy → release nhanh, an toàn\n\n## 3. Lộ trình thực hành (Practice Roadmap)\n\n1. Dockerize app đơn giản + compose với DB\n2. Deploy lên K8s local (minikube/kind) với Deployment + Service\n3. Dựng GitHub Actions: build → test → push image\n4. Terraform tạo VPC + EC2 trên AWS (free tier)\n5. Gắn Prometheus + Grafana theo dõi app\n\n## 📝 Checklist\n\n- [ ] Trả lời được các câu hỏi phỏng vấn DevOps cơ bản\n- [ ] Xử lý tình huống app chậm khi tăng tải\n- [ ] Có quy trình rollback khi deploy lỗi\n- [ ] Thực hành Docker → K8s → CI/CD → Terraform → Monitoring",
    "checklist": [
      "Trả lời được các câu hỏi phỏng vấn DevOps cơ bản",
      "Xử lý tình huống app chậm khi tăng tải",
      "Có quy trình rollback khi deploy lỗi",
      "Thực hành Docker → K8s → CI/CD → Terraform → Monitoring"
    ]
  }
];
