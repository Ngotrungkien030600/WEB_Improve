- Prefers strict 3-layer separation per feature: data (pure JSON objects) / logic (state + business rules, DOM-agnostic) / UI (DOM + render + events, imports logic). Each feature gets its own folder with `{feature}-logic.js` and `{feature}-ui.js` files. Names must be professional, descriptive, and consistent. Confidence: 0.9
- Prefers communication in Vietnamese. Confidence: 0.9
- Expects the assistant to review conversation history and project context before responding or making changes. Confidence: 0.8
- Prefers to be consulted (asked for confirmation) before the assistant makes significant structural/layout changes that affect how pages look, rather than the assistant implementing them unilaterally and the user having to reject them afterward. Confidence: 0.9
- For layout issues, communicates primarily through screenshots showing what looks wrong, then expects the assistant to diagnose and fix the root CSS cause (e.g., missing `flex-direction`, missing `min-height`, misplaced elements) without needing further verbal explanation. Confidence: 0.8
- Prefers compact floating UI widgets (e.g., timer) as small rectangles positioned fixed in a corner (bottom-right), with semi-transparent/blurred background, outside the main page content flow. Such widgets should only appear on the main/home page, not on sub-pages. Confidence: 0.8
- For interactive features like games, prefers each game to be on its own separate page (navigated via a link) rather than embedded as a tab/sub-panel within the main page. The main page should only show links/cards pointing to the dedicated game pages. Confidence: 0.9
- Prefers layouts that scale responsively to always fill the full viewport (full-screen), using flexible sizing (e.g., `clamp()`, percentage-based widths, `vw` units), generous container widths, proper responsive breakpoints, `min-height: 100dvh` to ensure backgrounds fill the screen, and correct `flex-direction` to avoid horizontal misalignment — no fixed card widths or overly narrow `max-width` containers that leave unused space on larger screens. Confidence: 0.9